---
phase: 06-inference-loop-validation
plan: 02
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - training/validate_inference.py
autonomous: false

must_haves:
  truths:
    - "Existing chuckles generate CLI works with vLLM endpoint by changing only settings.json"
    - "Fine-tuned model outputs are compared against base Qwen3-32B on identical prompts with phonetic scoring"
    - "User has a documented end-to-end workflow from data generation through model deployment"
    - "Both BitsAndBytes and AWQ quantization modes serve inference on RunPod without OOM, with measured VRAM usage"
  artifacts:
    - path: "training/validate_inference.py"
      provides: "End-to-end inference validation comparing fine-tuned vs base model, plus --print-settings for settings.json snippet"
      contains: "openai.OpenAI"
  key_links:
    - from: "training/validate_inference.py"
      to: "vLLM server on port 8000"
      via: "OpenAI client with base_url"
      pattern: "OpenAI.*base_url"
    - from: "training/validate_inference.py --print-settings"
      to: "settings.json (user's copy)"
      via: "Prints ready-to-paste settings.json snippet with model_name, api_base_url, api_key_env_var"
      pattern: "print-settings"
    - from: "settings.json (user's copy)"
      to: "vLLM server"
      via: "api_base_url field pointing to RunPod pod IP"
      pattern: "api_base_url"
---

<objective>
Create an inference quality validation script and verify the full generate-train-serve loop works end-to-end: the existing `chuckles generate` CLI connects to vLLM with only settings.json changes, and the fine-tuned model shows measurable improvement.

Purpose: Close the loop -- prove that training DPO actually improves parody generation quality, and that the entire workflow is reproducible.
Output: training/validate_inference.py (quality comparison script) + settings.json configuration example verified + end-to-end workflow documented in SUMMARY.
</objective>

<execution_context>
@/Users/patruff/.claude/get-shit-done/workflows/execute-plan.md
@/Users/patruff/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-inference-loop-validation/06-RESEARCH.md
@.planning/phases/06-inference-loop-validation/06-01-SUMMARY.md

# Integration points -- verify zero code changes needed
@src/chuckles_prime/model.py
@src/chuckles_prime/config.py
@src/chuckles_prime/cli.py

# Existing training scripts for pattern reference
@training/validate_merge.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create inference quality validation script</name>
  <files>training/validate_inference.py</files>
  <action>
Create `training/validate_inference.py` -- a standalone Python script that compares parody generation quality between the fine-tuned model and base Qwen3-32B. Follow the pattern of `training/validate_merge.py` (configuration constants at top, main() function, clear structured output).

**Script structure:**

1. **Configuration constants** at top:
   - `FINETUNED_URL = "http://localhost:8000/v1"` -- vLLM endpoint for fine-tuned model (user overrides via CLI arg)
   - `FINETUNED_MODEL = "chuckles-qwen3-32b-dpo"` -- must match --served-model-name
   - `BASE_URL = ""` -- base model endpoint (optional, user provides via CLI arg)
   - `BASE_MODEL = "Qwen/Qwen3-32B-AWQ"` -- or whatever the base model's served name is
   - `API_KEY = "not-needed"` -- dummy key for vLLM (no auth)

2. **Test prompts** -- a list of 10 movie titles covering diverse phonetic challenges:
   - Mix of short titles (Jaws, Frozen), medium (Pulp Fiction, Fight Club), and long (The Shawshank Redemption, Eternal Sunshine of the Spotless Mind)
   - These are input titles, not full prompts. The script wraps them in a parody generation prompt.

3. **Prompt template** -- match the system prompt and user prompt format used by the `chuckles generate` CLI. Reference the existing `PARODY_INSTRUCTIONS` or `DATASET_SYSTEM_PROMPT` from the codebase to stay consistent. The prompt should ask for a phonetically-sound parody of the given title.

4. **query_model(client, model_name, prompt) -> str** function:
   - Uses `openai.OpenAI(api_key=API_KEY, base_url=url)`
   - Sends chat completion with temperature=0.7, top_p=0.8, max_tokens=512
   - Returns the response text
   - Wraps in try/except, returns "[ERROR: ...]" string on failure

5. **score_parody(original_title, parody_text) -> dict** function:
   - Compute simple quality metrics (these don't need the full phonetic tools -- keep the script standalone):
     a. **Word count ratio**: len(parody.split()) / len(original.split()) -- closer to 1.0 is better (structure preservation)
     b. **Character similarity**: SequenceMatcher(None, original.lower(), parody.lower()).ratio() -- measures overall character overlap
     c. **Non-empty**: 1.0 if parody has content, 0.0 if empty/error
   - Return dict with these three scores

6. **`--print-settings` flag:**
   - When invoked with `--print-settings`, print a ready-to-paste settings.json snippet and exit (no server connection needed):
     ```json
     {
       "model_name": "chuckles-qwen3-32b-dpo",
       "api_base_url": "http://<RUNPOD_POD_IP>:8000/v1",
       "api_key_env_var": "VLLM_API_KEY"
     }
     ```
   - Replace `<RUNPOD_POD_IP>` with value from `--finetuned-url` if provided (extract host:port)
   - This makes the "zero code changes" promise explicit and documented -- user sees exactly what to put in settings.json

7. **main() function:**
   a. Parse CLI args: `--finetuned-url`, `--base-url` (optional), `--finetuned-model`, `--base-model`, `--print-settings`
   b. If `--print-settings`: print example settings.json snippet and exit
   c. Create OpenAI client(s) for fine-tuned model (and optionally base model)
   d. For each test title:
      - Query fine-tuned model, score the result
      - If base URL provided: query base model, score the result
      - Print side-by-side comparison
   e. Print summary table:
      - Average scores per model across all prompts
      - Per-prompt comparison if both models tested
   f. Print verdict: "PASS" if fine-tuned model has higher average character similarity than base, or if base model not tested, just print fine-tuned model scores

8. **Standalone mode (no base model):**
   - If `--base-url` not provided, only test the fine-tuned model
   - Print scores and example outputs
   - This is the default mode -- user may not have two models running simultaneously

9. **Dependencies comment** at top: `# Requires: pip install openai` (already in project)

IMPORTANT: Keep this script fully standalone -- no imports from chuckles_prime package. It runs on RunPod where the package may not be installed. Only use stdlib + openai.
  </action>
  <verify>
Verify script content:
- Contains `openai.OpenAI` usage with configurable base_url
- Contains at least 10 test prompts (movie titles)
- Contains scoring function with at least 2 metrics
- Contains summary output comparing models
- Contains `--print-settings` argument handling that outputs a settings.json snippet
- python -c "import ast; ast.parse(open('training/validate_inference.py').read())" passes
- Does NOT import from chuckles_prime (standalone)
  </verify>
  <done>
validate_inference.py exists, is valid Python, compares fine-tuned vs base model outputs on 10+ test prompts, computes quality scores, prints summary comparison table, supports --print-settings for settings.json snippet output, and runs standalone without chuckles_prime package dependency.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify end-to-end loop on RunPod</name>
  <what-built>
Complete inference serving and validation pipeline:
1. setup_inference.sh -- launches vLLM with fine-tuned model
2. quantize_awq.py -- optional AWQ quantization for faster inference
3. validate_inference.py -- quality comparison script
4. Zero-code-change CLI compatibility via settings.json

The full generate-train-serve loop is:
1. Generate data: `chuckles generate input.csv` (Phase 1-4)
2. Train DPO: `python train_dpo.py` on RunPod (Phase 5)
3. Merge + push: `python merge_and_push.py` on RunPod (Phase 5)
4. Serve: `bash setup_inference.sh --bnb` on RunPod (Phase 6)
5. Validate: `python validate_inference.py` on RunPod (Phase 6)
6. Generate better data: Update settings.json, run `chuckles generate` again (Phase 6)
  </what-built>
  <how-to-verify>
**Step 1: Launch vLLM with BitsAndBytes mode on RunPod**
- SSH into a RunPod GPU pod (A6000 48GB recommended)
- Copy training/setup_inference.sh to pod
- Run: `bash setup_inference.sh --bnb`
- Wait for "Server started" message (may take 5-10 minutes for model download + quantization)
- Test: `curl http://localhost:8000/v1/models` -- should list "chuckles-qwen3-32b-dpo"
- Record VRAM usage: `nvidia-smi --query-gpu=memory.used,memory.total --format=csv`
- Confirm NO OOM errors in vLLM output (search for "CUDA out of memory" or "OOM")
- Send a test inference request to confirm coherent output:
  `curl http://localhost:8000/v1/chat/completions -d '{"model":"chuckles-qwen3-32b-dpo","messages":[{"role":"user","content":"Generate a parody of Jaws"}],"max_tokens":128}'`
- Stop the server (Ctrl+C)

**Step 2: Test AWQ quantization mode on RunPod**
- Run: `python quantize_awq.py` (produces AWQ model locally + pushes to Hub)
- Verify quantize_awq.py validation step prints coherent output (not garbled)
- Run: `bash setup_inference.sh --awq`
- Wait for "Server started" message
- Record VRAM usage: `nvidia-smi --query-gpu=memory.used,memory.total --format=csv`
- Confirm VRAM usage is LOWER than BitsAndBytes mode (AWQ should use less VRAM)
- Confirm NO OOM errors in vLLM output
- Send same test inference request as Step 1, confirm coherent output
- Compare: AWQ output quality should be comparable to BitsAndBytes (not garbled)

**Step 3: Run validation script**
- With vLLM still running (either --bnb or --awq mode):
- Copy training/validate_inference.py to pod
- Run: `python validate_inference.py --finetuned-url http://localhost:8000/v1`
- Verify: Script produces quality scores for 10+ test prompts
- Verify: Parody outputs are coherent (not garbled/empty)
- Run: `python validate_inference.py --print-settings` and verify it outputs a valid settings.json snippet

**Step 4: Test CLI compatibility (on local machine)**
- Update your local settings.json (use output from --print-settings as guide):
  ```json
  {
    "model_name": "chuckles-qwen3-32b-dpo",
    "api_base_url": "http://<RUNPOD_POD_IP>:8000/v1",
    "api_key_env_var": "VLLM_API_KEY"
  }
  ```
- Set env var: `export VLLM_API_KEY="not-needed"`
- Run: `chuckles generate test_input.csv` (with a small CSV of 2-3 titles)
- Verify: Generation completes without errors, produces parody candidates

**Step 5: Verify quality (subjective)**
- Read the generated parodies -- do they sound phonetically similar to originals?
- Compare to outputs from Phase 2 (base model) -- any improvement noticeable?

**Quantization verification summary to report:**
- BitsAndBytes VRAM usage: ___GB / ___GB total
- AWQ VRAM usage: ___GB / ___GB total
- BitsAndBytes OOM: Yes/No
- AWQ OOM: Yes/No
- BitsAndBytes output coherent: Yes/No
- AWQ output coherent: Yes/No
  </how-to-verify>
  <resume-signal>Type "approved" with notes on quality observations, or describe issues encountered</resume-signal>
</task>

</tasks>

<verification>
1. `python -c "import ast; ast.parse(open('training/validate_inference.py').read())"` -- syntax check passes
2. `grep -c "openai" training/validate_inference.py` returns >= 1
3. `grep -c "chuckles_prime" training/validate_inference.py` returns 0 (standalone, no package imports)
4. `grep -c "SequenceMatcher\|score" training/validate_inference.py` returns >= 1
5. Human verification: vLLM serves model, CLI connects, parodies generated
</verification>

<success_criteria>
- validate_inference.py compares fine-tuned model output quality on 10+ test prompts
- Existing `chuckles generate` CLI works with vLLM endpoint by changing only settings.json (zero code changes verified)
- End-to-end loop documented: generate -> train -> merge -> serve -> validate -> generate better
- Human verifies on RunPod that the full loop works
</success_criteria>

<output>
After completion, create `.planning/phases/06-inference-loop-validation/06-02-SUMMARY.md`
</output>
